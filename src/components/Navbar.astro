---
const links = [
    { href: "/", label: "Home" },
    { href: "/portfolio", label: "Portfolio" },
    { href: "/about", label: "About" },
    { href: "/contact", label: "Contact"},
];    
---

<nav class="flex justify-between items-center py-4 px-6 bg-zinc-900 text-white rounded-2xl shadow-lg mb-8">
    <div class="text-xl font-bold tracking-wide">Dom the Technical Writer</div>
    <ul class="flex gap-6">
        {links.map(link => (
            <li><a href={link.href} class="hover:text-blue-400 transition">{link.label}</a></li>
        ))}
    </ul>
</nav>